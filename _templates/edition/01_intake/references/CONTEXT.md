# 01_intake references

This is the stage's Layer 3 boundary. Keep only stable intake rules or readable
pointers explicitly named by the parent `CONTEXT.md`. Publication rules remain
canonical in the selected publication profile. Request-specific material and
stage results belong in `output/`, not here.
