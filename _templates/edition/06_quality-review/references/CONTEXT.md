# 06_quality-review references

This is the stage's Layer 3 boundary. Keep only stable quality or risk rules or
readable pointers explicitly named by the parent `CONTEXT.md`. Publication
rules remain canonical in the selected publication profile. Current findings
and stage results belong in `output/`, not here.
